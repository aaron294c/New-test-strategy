name: Update Swing Snapshots

on:
  workflow_dispatch:
  schedule:
    # US Market hours (EST/EDT timezone)
    - cron: "30 14 * * 1-5" # 9:30 AM ET - Market Open
    - cron: "0 18 * * 1-5"  # 1:00 PM ET - Mid-Session
    - cron: "0 21 * * 1-5"  # 4:00 PM ET - Market Close

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          persist-credentials: true

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"
          cache-dependency-path: backend/requirements.txt

      - name: Install backend dependencies
        run: pip install -r backend/requirements.txt

      - name: Compute snapshots
        run: python scripts/compute_swing_snapshots.py --pretty

      - name: Commit and push (if changed)
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          # Pull latest changes first
          git pull origin main

          # Regenerate snapshots with latest code
          python scripts/compute_swing_snapshots.py --pretty

          # Check if there are changes after regeneration
          if [ -z "$(git status --porcelain)" ]; then
            echo "No snapshot changes to commit."
            exit 0
          fi

          git add backend/static_snapshots/swing_framework
          git commit -m "chore: update swing snapshots"
          git push

